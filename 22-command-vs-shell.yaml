- name: command vs shell
  hosts: localhost
  connection: local

  tasks: 
    - name: run command
      ansible.builtin.command: ls -ltr
      register: result

    - name: print output
      ansible.builtin.debug:
        msg: "{{ result.stdout }}"


