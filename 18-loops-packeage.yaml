- name: package install
  hosts: web
  become: yes

  tasks:
  - name: print packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql-server
    - nginx
    - postfix  